# substackengine

## Repository status

This repository is currently **empty** — no source files, dependency manifests, Dockerfiles, Compose files, or configuration have been committed (0 files, 0 commits at onboarding time, 2026-09-17). No application, runtime, or toolchain can be identified, and none is assumed.

## What this means for agents

- There is nothing to build, run, test, or map yet. Do not invent commands, ports, or stack claims for this repository.
- Stack details, run/test commands, and a codebase map will be filled in by the next onboarding run once code lands here.
- Repo policy (default branch, merge method) lives in [`config.yml`](./config.yml).

_Generated by the Obvious onboarding worker via the empty-repository scaffold path._
